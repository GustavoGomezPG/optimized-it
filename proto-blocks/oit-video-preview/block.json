{
  "$schema": "https://schemas.wp.org/trunk/block.json",
  "apiVersion": 3,
  "name": "proto-blocks/oit-video-preview",
  "version": "1.0.0",
  "title": "OIT Video Preview",
  "category": "optimizedit",
  "icon": "video-alt3",
  "description": "Click-to-play video card. Large rounded thumbnail with a centered play button; click opens a fullscreen lightbox with the player. Accepts YouTube, Vimeo, or self-hosted MP4. Thumbnails are auto-fetched from YouTube/Vimeo; MP4 requires a custom thumbnail.",
  "keywords": ["video", "youtube", "vimeo", "lightbox", "play", "oit"],
  "supports": {
    "html": false,
    "anchor": true,
    "customClassName": true,
    "align": ["wide", "full"]
  },
  "protoBlocks": {
    "version": "1.0",
    "useTailwind": true,
    "template": "template.php",
    "fields": {},
    "controls": {
      "source": {
        "type": "select",
        "label": "Video source",
        "default": "youtube",
        "options": [
          { "key": "youtube", "label": "YouTube" },
          { "key": "vimeo",   "label": "Vimeo"   },
          { "key": "mp4",     "label": "MP4 (self-hosted)" }
        ]
      },
      "videoFile": {
        "type": "video",
        "label": "Video file (from Media Library)",
        "allowedTypes": ["video"],
        "help": "Pick an uploaded video from the media library.",
        "conditions": { "visible": { "source": ["mp4"] } }
      },
      "videoUrl": {
        "type": "text",
        "label": "Video URL or ID",
        "default": "",
        "help": "Paste the full URL or just the video ID.",
        "conditions": { "visible": { "source": ["youtube", "vimeo"] } }
      },
      "customThumbnail": {
        "type": "image",
        "label": "Custom thumbnail",
        "help": "Required for self-hosted video (YouTube/Vimeo thumbnails are auto-fetched).",
        "conditions": { "visible": { "source": ["mp4"] } }
      }
    }
  }
}
